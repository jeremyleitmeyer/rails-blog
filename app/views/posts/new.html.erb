<div class="posts-new">
	<% if current_user %>
		<%= form_for Post.new do |f| %>
			<%= f.label :title %>
			<%= f.text_field :title %>

			<%= f.label :content %>
			<%= f.text_area :content %>

			<%= f.submit %>
		<% end %>
	<% end %>
</div>