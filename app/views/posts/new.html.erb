<div class="posts-new">
	<% if current_user %>
		<%= bootstrap_form_for Post.new do |f| %>
			<%= f.text_field :title %>
		<br>
			<%= f.text_area :content %>
		<br>
		<%= f.submit %>
		<% end %>
	<% end %>
</div>