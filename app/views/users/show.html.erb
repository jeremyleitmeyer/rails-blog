<div class="users-show">
	<% if current_user %>
	<h1><%= @user.username %></h1>
		<p>
			<p>E-mail:
			<%= @user.email %>
			</p>
			<br>
			<%= link_to "Edit", edit_user_path(@user) %>
		</p>
	<% end %>
</div>

<!-- users profile page -->